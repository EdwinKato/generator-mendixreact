import { ShallowWrapper, shallow } from "enzyme";
import { DOM, createElement } from "react";

import { WidgetName, ProgressBarProps } from "./../WidgetName";


describe("WidgetName", () => {

    beforeEach(() => {
        // Initialisation code before each test should be placed here
    });

    it("spec", () => {
        // spec test should be put here
    });

});
